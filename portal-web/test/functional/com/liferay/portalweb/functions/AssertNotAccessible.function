@default = "assertNotAccessible"
definition {

	function assertNotAccessible {
		WaitForSPARefresh();

		selenium.assertNotAccessible();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}

}