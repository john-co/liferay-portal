@default = "waitForElementPresent"
definition {

	function waitForElementPresent {
		selenium.waitForElementPresent();
	}

	function waitForLastJavaScript {
		selenium.waitForElementPresent("xpath=(//script)[last()]");
	}

}