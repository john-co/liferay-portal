#!/usr/bin/env node

'use strict';

Liferay.Loader.define('liferay-js-toolkit-showcase$loose-envify@1.4.0/cli', ['module', 'exports', 'require', '.', 'liferay-js-toolkit-showcase$fs'], function (module, exports, require) {
  var define = undefined;
  var looseEnvify = require('.');
  var fs = require('liferay-js-toolkit-showcase$fs');

  if (process.argv[2]) {
    fs.createReadStream(process.argv[2], { encoding: 'utf8' }).pipe(looseEnvify(process.argv[2])).pipe(process.stdout);
  } else {
    process.stdin.resume();
    process.stdin.pipe(looseEnvify(__filename)).pipe(process.stdout);
  }
});
//# sourceMappingURL=cli.js.map